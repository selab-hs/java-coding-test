# 비밀지도
* [링크](https://school.programmers.co.kr/learn/courses/30/lessons/17681)
* 시간 : 10분

```java
    class Solution {
        public String[] solution(int n, int[] arr1, int[] arr2) {
        String[] answer = new String[n];
        int[][] binaryArr1 = new int[n][n];
        int[][] binaryArr2 = new int[n][n];

        for (int i = 0; i < n; i++) {
            String binaryString = Integer.toBinaryString(arr1[i]);
            while (binaryString.length() < n) {
                binaryString = "0" + binaryString;
            }
            for (int j = 0; j < binaryString.length(); j++) {
                binaryArr1[i][j] = binaryString.charAt(j) - '0';
            }
        }

        for (int i = 0; i < n; i++) {
            String binaryString = Integer.toBinaryString(arr2[i]);
            while (binaryString.length() < n) {
                binaryString = "0" + binaryString;
            }
            for (int j = 0; j < binaryString.length(); j++) {
                binaryArr2[i][j] = binaryString.charAt(j) - '0';
            }
        }

        for (int i = 0; i < n; i++) {
            answer[i] = "";     //이걸 해주지 않으면 null### 같이 리턴됨
            for (int j = 0; j < n; j++) {
                if(binaryArr1[i][j] == 1 | binaryArr2[i][j] == 1){
                    answer[i] += "#";
                }else answer[i] += " ";
            }
        }

        return answer;
    }
    }
```

# 배운 것
* 문자열이 null인데 +로 문자열을 더하면 null#### 같은 값이 나옴